# Specification

## Summary
**Goal:** Update the app domain to "Cutiepie" and republish so deployment succeeds under the new domain.

**Planned changes:**
- Change the configured project/app domain name to exactly "Cutiepie".
- Rerun publish/deployment and ensure domain validation passes without errors.

**User-visible outcome:** The site is successfully published/deployed under the "Cutiepie" domain.
